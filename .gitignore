# Секреты и runtime (API)
api/v2/farms/config.json
api/v2/requests.log
api/v2/market/*.cache.json
api/v2/market/wallets.json
api/v2/market/scrape-*.log

# Локальная копия Flutter SDK (ставится вручную, не в репозиторий)
flutter/

# Python (venv не в корне репозитория)
.venv/

# OS
Thumbs.db
.DS_Store
